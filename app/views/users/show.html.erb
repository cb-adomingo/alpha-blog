<h1 class="display-6 center mt-4">Welcome to <%= @user.username %>'s page</h1>
<div class="row">
    <div class="col-md-12 col-md-offset-4 center">
        <%= gravatar_for @user, size: 150 %>
    </div>
</div>
<h4 class="center mt-2"><%= @user.username %>'s articles</h4>
<%= will_paginate @user_articles, list_classes: %w(pagination justify-content-center mt-4) %>
<%= render 'articles/article', obj: @user_articles %>
<%= will_paginate @user_articles, list_classes: %w(pagination justify-content-center mt-4) %>
